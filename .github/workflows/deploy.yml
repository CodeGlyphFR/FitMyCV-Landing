name: CI/CD Pipeline

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
    types: [opened, synchronize, reopened]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: eds-78/fitmycv-landing

jobs:
  quality-check:
    name: 🛡️ Quality & Build
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: 'npm' }
      - run: npm ci
      - run: npm run lint
      - run: npm run build

  deploy:
    name: 🚀 Deploy
    needs: quality-check
    if: always() && (needs.quality-check.result == 'success' || github.event_name == 'push')
    runs-on: ubuntu-latest
    permissions: { contents: read, packages: write }
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver: docker-container

      - name: Log in to Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GH_PAT }}

      - name: Set Env (Staging vs Prod)
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            echo "TAG=staging-pr-${{ github.event.number }}" >> $GITHUB_ENV
            echo "TARGET_DIR=/home/erickdesmet/STAGING_SAAS/FitMyCV/landing" >> $GITHUB_ENV
            echo "COMPOSE_FILE=docker-compose.staging.yml" >> $GITHUB_ENV
            echo "IS_PROD=false" >> $GITHUB_ENV
          else
            # On cherche le numéro de la PR dans le message de commit (ex: "Merge pull request #6")
            PR_NUMBER=$(git log -1 --pretty=%s | grep -oP '(?<=#)\d+' || echo "")
            echo "TAG=latest" >> $GITHUB_ENV
            echo "SRC_TAG=staging-pr-$PR_NUMBER" >> $GITHUB_ENV
            echo "TARGET_DIR=/home/erickdesmet/PROD_SAAS/FitMyCV/landing" >> $GITHUB_ENV
            echo "COMPOSE_FILE=docker-compose.prod.yml" >> $GITHUB_ENV
            echo "IS_PROD=true" >> $GITHUB_ENV
          fi

      # ÉTAPE 1 : Build (Staging uniquement)
      - name: Build & Push Staging
        if: env.IS_PROD == 'false'
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.TAG }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # ÉTAPE 2 : Promotion (Prod uniquement - Ultra rapide)
      - name: Promote Image to Production
        if: env.IS_PROD == 'true'
        uses: akhilerm/tag-push-action@v2.1.0
        with:
          src: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.SRC_TAG }}
          dst: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest

      # ÉTAPE 3 : Déploiement SSH
      - name: Deploy SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          port: 1702
          script: |
            echo "${{ secrets.GH_PAT }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            cd ${{ env.TARGET_DIR }}
            IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.TAG }}"
            docker pull $IMAGE
            sed -i "s|image: ghcr.io/.*|image: $IMAGE|g" ${{ env.COMPOSE_FILE }}
            docker compose -f ${{ env.COMPOSE_FILE }} up -d --remove-orphans
            docker image prune -f
